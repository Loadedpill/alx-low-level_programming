#include <stdio.h>
/**
 * main - Entry point
 * Description: checks on the last digit of the assigned var
 * Return: Always 0 (success)
 **/
int main(void)
{
char ch;
for (ch = 'a' ; ch <= 'Z' ; ch++)
{
putchar(ch);
}
return(0);
}
