#include <stdio.h>
#include <unistd.h>
/**
 * main -Entry point
 * Description: prints allsingle digit numbers of
 * base 10 from 0
 * Return: Always 0 (success)
 **/
int main(void)
{
int b;
for (b = 0; b < 10; b++)
{
printf("%d", n);
}
putchar('\n');
return (0);
}
